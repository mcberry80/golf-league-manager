# Golf League Manager - Environment Variables Example
# Copy this file to .env and fill in your values (DO NOT commit .env to Git)

# ============================================
# REQUIRED VARIABLES
# ============================================

# Your Google Cloud Project ID
GCP_PROJECT_ID=your-gcp-project-id

# Clerk Secret Key for authentication (get from clerk.com dashboard)
CLERK_SECRET_KEY=sk_test_your_clerk_secret_key_here

# ============================================
# OPTIONAL VARIABLES
# ============================================

# Server port (default: 8080)
PORT=8080

# Deployment environment: dev, staging, production (default: production)
ENVIRONMENT=dev

# Log level: DEBUG, INFO, WARN, ERROR (default: INFO)
LOG_LEVEL=DEBUG

# CORS allowed origins (comma-separated, default: *)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Application version for health checks
APP_VERSION=1.0.0

# ============================================
# RECOMMENDED: Use Secret Manager Instead
# ============================================
# For better security, use Google Cloud Secret Manager:
#   .\setup-secrets.ps1 -ProjectId "your-project-id" -ClerkSecretKey "sk_test_..."
#
# Then load secrets with:
#   .\load-secrets-local.ps1 -ProjectId "your-project-id"
#
# Or use the quick start script:
#   .\start-dev.ps1 -ProjectId "your-project-id"
#
# See SECRET_MANAGEMENT.md for more details.
